.content
	ol
		li
			a(href='./stream') Open a second window
			= " and link it to "
			a(href='./showimage', target='_blank') ./showimage
		li Click to any image to push its data
		li Check result in&nbsp;
			a(href='./stream') second window
			br
			br
	
	pathes = [ 'images/webcam.jpeg' ]
	
	ul#imglist
		each path, i in pathes
			li
				img.my-image(src='#{path}', title='Pic no. #{i}')

:coffeescript
	$(document).ready ->
		HomeController = require "controllers/homecontroller"
		controller = new HomeController();
